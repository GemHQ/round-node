
module.exports = class Payment

  constructor: (@resource) 

  # sign: (wallet, callback) ->
  #   unless wallet
  #     throw Error('A wallet is required to sign a transaction')

      # transaction = CoinOP.bit.transaction.data